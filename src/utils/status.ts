/**
 * @enum Status Enum.
 * @returns an enum of type Status.
 * @description Enum with different types of status.
 */

enum Status {
  Draft = 'Draft',
  Pending = 'Pending',
  Approved = 'Approved',
  Cancelled = 'Cancelled'
}
